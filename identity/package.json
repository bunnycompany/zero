{
  "name": "@0-computer/identity",
  "version": "0.1.0",
  "private": true,
  "description": "0.computer identity: anonymous passkey registration + auth. No email, no password, no PII — a handle and a device-bound key, ever.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "echo 'Not wired yet — deploy is a deliberate, separate, human-approved step.' && exit 1",
    "test": "node --test test/"
  },
  "dependencies": {
    "@passwordless-id/webauthn": "2.4.0"
  },
  "devDependencies": {
    "wrangler": "4.121.0",
    "@cloudflare/workers-types": "5.20260812.1"
  }
}
