# Her — the 8-hour plan, 2026-09-10

Written at 11:40 UTC with a hard stop at 19:30 UTC. The founder is not at
the keyboard. Everything here runs in parallel threads; the cloud session
integrates and pushes at checkpoints. This file is updated at each one, so
whoever reads it next knows what landed and what did not.

## Ground truth going in

- Three states of Zero exist: the Node prototype (`dangerceo/zero`, March 2026,
  the code the earlier HerApp documents were written against), the Python
  rewrite snapshot (`bunnycompany/zero` main, August 2026, the only tree the
  cloud session was given and the one this branch is built on), and the
  founder's local code on the MacBook Neo, ahead of both and unpushed. The
  founder never used the Python Zero and does not want it as the product.
- What is worth keeping from the Python tree is language-independent: the
  namespace file contract, the trust invariants and their tests, the docs
  (failure ledger, user stories with checks, proactivity, reaching-zero), and
  Her's design (`docs/her.md`, `docs/her-lineage.md`).
- CI on this branch compiles the Swift menubar app (macOS runner) and the
  Android plain flavor (Linux, JDK 21, SDK 36) and runs the Python suite:
  run 34470871995, all green.

## Threads

| thread | what | lands as |
|---|---|---|
| audit workflow | 10 reviewers, 3 sceptics per defect, fixers per area, four handoff docs, critic | edits on this branch + `docs/her-handoff.md`, `docs/her-production-readiness.md`, `docs/her-runbook.md` |
| feature swarm | 12 stories (natural-language times, wider read confinement, day report, disk-low guard, preflight, proactivity backtest, memory R2, ship-review fixes, prompt hardening, Android history, honest liveness, bridge carry-forwards) + intake deck by judge panel + threat model + Her north star | verified patches in the scratchpad, integrated at checkpoint 2 |
| Node port | `her-node/`: ns, memory, intake, presence, devices+bridge, CLI, interop tests, CI job | one combined patch, integrated at checkpoint 3 |
| extras | ten-day journey test, Ray-Ban Display web-app conformance from Meta docs, Glyph SDK verification from Nothing's example project, pitch refresh | verified patches, integrated at checkpoint 2 or 3 |
| the Mac (Routine into the founder's Remote Control session) | describe the real local Zero (read-only, reported privately); model-layer eval; `nothing` flavor if the aar exists | a message to the cloud session, or a file the founder pastes |

## Checkpoints

| when (UTC) | what | status |
|---|---|---|
| ~13:00 | CP1: audit lands; commit, push, CI | pending |
| ~14:30 | CP2: swarm + extras patches integrated in dependency order; suites green; push; CI | pending |
| ~15:30 | CP3: Node core integrated; its CI job green; push | pending |
| ~17:00 | CP4: docs reconciled with reality (CI facts, Node port, lineage, `.gitignore` for `.claude/worktrees`); completeness critic; "start here" at the top of README; push | pending |
| 17:00–19:30 | buffer: CI red, Mac report follow-ups, final handoff message | pending |

## What only the founder can do, whenever

- Push the Neo's local Zero to a branch so the next session can re-plan Her
  against the real code (the cloud cannot see it).
- Publish `@0-computer/zero` then `@0-computer/her`.
- Apple Developer ID and notarization; Meta Developer Mode on the glasses;
  the Glyph Matrix aar on the phone.
